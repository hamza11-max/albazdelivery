// prisma-shims.d.ts — shim removed.
// This file previously contained a temporary `declare module '@prisma/client'` shim
// that overrode the generated Prisma client types. It has been emptied so the
// TypeScript server can resolve the real generated types from
// `node_modules/@prisma/client` after running `npm ci` and `npx prisma generate`.

// If you still see missing Prisma types in your editor, run:
//   npm ci
//   npx prisma generate
// and restart your TS server/IDE.

export {}
