# Changelog

All notable production-oriented changes are summarized here. For deep audits and phased delivery status, see [docs/TECHNICAL_PROJECT_AUDIT.md](docs/TECHNICAL_PROJECT_AUDIT.md).

## [Unreleased]

### Documentation & engineering phases (2026-Q2)

- **Phase A — Platform integrity:** async rate limits, subscription GET/STARTER rules, canonical root `app/api` re-exports for legacy nested routes, guest-order deployment guard on Vercel, Stripe subscription webhook idempotency, minimal API + auth e2e. Verification: `npm run verify:phase-a`.
- **Phase B — Product hardening:** operational docs (notifications, Redis/BullMQ, ERP vs storefront catalog), Stripe webhook structured logging (`money_path_v1`) and Sentry capture on failures, CSRF on additional admin mutations. Verification: `npm run verify:phase-b`.
- **Phase C — Excellence:** single changelog + architecture snapshot; legacy root `FINAL_*` / `COMPLETE_*` marketing docs superseded here; demand-prediction API declares methodology; expanded Playwright boundaries. Verification: `npm run verify:phase-c`.
- **Phase 1 (Enhancement Plan):** Stripe webhook handlers for order `payment_intent.succeeded` / `payment_intent.payment_failed` (persists `Payment` rows; still deduped by `ProcessedStripeWebhookEvent`); admin `PATCH /api/admin/refunds/[id]` to complete/reject refunds with Stripe idempotency `admin-refund-{id}` for card payments (`Refund.stripeRefundId`); `VendorStaffMember` + server-side vendor scope on orders list/detail/PATCH and ERP inventory; checkout client aligned with `successResponse` envelope + cent amounts for `create-intent` / `formatPrice`; `scripts/verify-deploy-env.mjs` for production deploy gates (`npm run verify:deploy-env`). Verification: `npm run verify:phase-1` (migrate DB for new models/columns first).
- **Phase 2 (hardening):** Optional BullMQ offload for Stripe webhooks (`STRIPE_WEBHOOK_USE_QUEUE=1`, queue `stripe-webhooks`, `lib/stripe-webhook-dispatch.ts`, `npm run worker:stripe-webhook`); `GET /api/health` reports dependency posture without marketing “100%” claims; POS/ERP `POST` sales validate inventory stock before decrement (`lib/erp/pos-sale-stock.ts`, mirrored under `apps/vendor`); notification channel matrix module + doc table; production deploy env warns when Upstash rate-limit Redis is missing (`STRICT_PRODUCTION_RATE_LIMIT=1` to fail); `docs/API_WORKSPACE_PARITY.md` + expanded `docs/BACKGROUND_JOBS_AND_REDIS.md`. Verification: `npm run verify:phase-2`.
- **Phase 3 (enterprise slice):** Financial audit helper (`lib/security/financial-audit.ts`) + refund PATCH audit events; admin `GET|POST /api/admin/finance/payouts` with CSV `?format=csv` and mirrored under `apps/admin`; `GET /api/user/data-export` (authenticated JSON export, no password hash); `docs/DEPLOYMENT_TOPOLOGY.md`, `docs/REALTIME_STRATEGY.md`, `docs/COMPLIANCE_DATA_SUBJECTS.md`; Dependabot weekly npm PRs; PR workflow runs `verify:phase-3`. Verification: `npm run verify:phase-3`.
- **Phase 4 (plan leftovers):** Vendor inventory **CASHIER vs MANAGER** enforcement; seeded cashier/manager staff users; `GET /api/admin/ops/metrics`; `POST /api/user/account/anonymize` + audit; `logNotificationChannelPlan` on internal order create; root `app/api/admin/webauthn-passkeys` mirrors; scripts `verify-admin-csrf.mjs`, `verify-api-parity.mjs`, `verify-money-no-test-skip.mjs`; `docs/GUEST_ORDERS_POLICY.md`, `SECURITY_PROGRAM.md`, `POSTGRES_RLS_BACKLOG.md`, `PAYOUT_PROVIDER_BACKLOG.md`; PR workflow `verify:phase-4` + soft `npm audit` critical. Verification: `npm run verify:phase-4`.
