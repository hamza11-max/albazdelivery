/**
 * Design system tokens – single import for UI components.
 * Matches web ALBAZ design (apps/customer globals.css).
 */
export {
  colors,
  spacing,
  typography,
  borderRadius,
  shadows,
} from './colors';
